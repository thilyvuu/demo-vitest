import { afterEach, beforeEach } from "vitest";

// Optional: Add your custom matchers here if needed
// import '@testing-library/jest-dom';

beforeEach(() => {
  // Setup global configurations here, if any
});

// Optional: If you're using CSS modules or need specific styles
// config.global.stubs = {
//   // Stub components that you want to mock
// };

afterEach(() => {
  // Clean up after each test (e.g., resetting mocks)
});
